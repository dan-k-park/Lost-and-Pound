import React from "react";
import styled from "styled-components";

const Container = styled.div`
  display: flex;
`;

const Profile = () => {
  return <Container>Portflie</Container>;
};

export default Profile;
