import React, { useState } from "react";
import { Modal as BaseModal } from "react-modal";

//The modal is open when

const Modal = () => {
  return <BaseModal></BaseModal>;
};

export default Modal;
